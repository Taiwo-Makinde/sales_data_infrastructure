# Auto detect text files and perform LF normalization
* text=auto

# Force GitHub to recognize SQL files while not ignoring the Python stats
*.sql linguist-detectable=true
*.sql linguist-language=SQL
